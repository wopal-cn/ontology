---
description: 基于代码库深度分析创建详尽的功能实现计划 (Plan)
---

# 规划新功能 (Plan Feature)

## 功能目标: $ARGUMENTS

## 核心使命

通过系统的代码库分析、外部研究和战略规划，将用户的功能请求转化为**详尽可执行的实现计划**。

**核心原则**: 
- 我们在规划阶段**绝不编写实现代码**
- 我们的目标是创建一个富含上下文的实施计划，让执行阶段的 AI Agent 能够**一次性成功实现**

**核心理念**: 
- "上下文就是王道"。计划中必须包含实现所需的所有信息：代码模式、必读文档、验证命令等。

---

## 规划流程

### Phase 0: 前置上下文检查

**确定目标项目：**
- [ ] 检测当前工作目录所在的项目
- [ ] 读取目标项目的 `AGENTS.md`

### Phase 1: 需求理解

**深度分析特性:**
- 提取要解决的核心问题
- 识别用户价值和业务影响
- 确定功能类型：新功能 / 增强 / 重构 / Bug修复
- 评估复杂度：低 / 中 / 高
- 梳理受影响的系统和组件

如果需求模糊，请在进行到下一步之前向用户提问澄清（如：库的偏好、特定实现方式、架构决策）。

### Phase 2: 代码库情报收集

> **提示**: 充分利用 `glob`、`grep` 和 `read` 工具并行探索代码库。

**1. 架构与结构分析**
- 定位相关服务、组件的边界与集成点
- 查找需要修改的现有文件（路由、控制器、组件等）
- 定位配置文件（`package.json`, `pyproject.toml` 等）以确定已有依赖
- **禁止自行预设依赖**，以当前项目真实情况为准

**2. 模式识别**
- 在代码库中寻找类似功能的实现作为参考
- 识别并提取项目的编码规范：
  - 命名风格（组件名、API端点、数据库模型）
  - 错误处理和日志记录模式
  - 目录结构规则
- 提取该领域的常见通用模式，**并显式记录需要避免的反模式**

**3. 测试模式分析**
- 确定现有的测试框架和测试组织结构（如 pytest, jest）
- 寻找可供模仿的测试用例（区分单元测试与集成测试模式）
- 记录覆盖率要求和测试标准

**4. 依赖分析**
- 梳理需要用到的相关外部库
- 了解库是如何被集成的（查看 imports、配置）
- 注意库的特定版本和兼容性要求

**5. 集成点分析**
- 识别需要修改/注册的已有文件（如 Router、API 注册表、依赖注入容器）
- 分析数据库模型模式及迁移规则（如适用）
- 识别相关的认证与授权拦截器模式

### Phase 3: 外部研究

如果涉及新库或新 API，优先收集其最新的官方文档要求：
- 查找当前版本的最佳实践
- 收集实现示例和注意避坑点 (Gotchas)
- 在生成的计划中记录具体的文档链接及其特定锚点段落。

### Phase 4: 战略思考

在写计划前进行深度思考：
- 新功能如何融入现有架构？
- 关键依赖是什么？操作的先后顺序是什么？
- 哪里最容易出问题？（如：并发、空指针、边界情况）
- 如何全面测试这个功能？是否存在性能与安全隐患？

### Phase 5: 生成计划并输出

根据探索与思考的结果，生成功能计划文档。

---

## Plan 模板结构

> **执行者须知**: 以下计划应当完整且可执行，但在开始实现前，请务必验证文档、代码库模式和任务的合理性。

### 1. 基础元数据

- **特性类型**: [新功能/增强/重构/Bug修复]
- **估算复杂度**: [低/中/高]
- **受影响系统**: [列出主要组件/服务]
- **核心依赖**: [所需的外部库或服务]

### 2. 功能概述

**面临问题**: [描述要解决的核心问题]

**解决方案**: [描述建议的解决方案架构及如何解决该问题]

**用户故事**:
```
作为 [用户类型]
我想要 [动作/目标]
以便于 [收益/价值]
```

### 3. 上下文参考

**现有代码参考**:
- `path/to/file.py` (行 15-45) - **原因**: 包含我们将要模仿的 X 模式

**需要创建的新文件**:
- `path/to/new_service.py` - 用于 X 功能的服务实现

**参考文档**:
- [文档链接](https://example.com/doc1#section) - 特定章节及原因

### 4. 需遵循的代码模式

- **命名约定**: [例如：服务类必须以 Service 结尾]
- **错误处理**: [例如：使用统一的 ApiError 抛出]
- **日志记录**: [例如：使用 logger.info 记录关键节点]

### 5. 阶段实现计划

将功能按逻辑阶段划分（基础、核心、集成、测试等），每个阶段列出目标任务, 无需时间估算。

**示例**:
- Phase 1: 基础构建 - 设置基础结构、配置依赖
- Phase 2: 核心实现 - 实现核心业务逻辑
- Phase 3: 集成与测试 - 注册组件、编写测试

### 6. 详细任务拆解

按从上到下的顺序执行每个任务，任务必须是原子可独立测试的。

**任务前缀**: CREATE(新建), UPDATE(更新), ADD(新增), REMOVE(移除), REFACTOR(重构), MIRROR(模仿)

**任务格式**:
```
### T1: {ACTION} {target_file}

- [ ] **状态**: 待处理 / 进行中 / 已完成
- **IMPLEMENT**: {具体的实现细节}
- **PATTERN**: {参考现有模式 - file:line}
- **IMPORTS**: {需要的导入和依赖}
- **GOTCHA**: {需要避免的已知问题或陷阱}
- **VALIDATE**: `{可执行的验证命令}`
- **执行记录**: {AI执行时填写}
```

### 7. 测试策略

- **单元测试**: [范围和要求]
- **集成测试**: [范围和要求]
- **边界用例**: [列出边界情况]

### 8. 验证命令

| 验证层级 | 执行命令 | 预期结果 |
|---------|---------|---------|
| Level 1 (语法与风格) | `[lint/format 命令]` | 无警告/错误 |
| Level 2 (单元测试) | `[单元测试命令]` | 所有测试通过 |
| Level 3 (集成测试) | `[集成测试命令]` | 工作流无中断 |
| Level 4 (手动验证) | [验证方式] | 表现符合预期 |

### 9. 验收标准

- [ ] 功能实现了所有指定的需求
- [ ] 所有验证命令执行通过且无错误
- [ ] 代码遵循项目的约定和模式
- [ ] 没有破坏现有的功能回归

---

## 文件命名与存放位置

> **智能归属**: 本项目采用 Monorepo 架构，计划文件必须根据功能归属智能选择存放位置。

### 归属判断逻辑

**1. 项目计划** → `<project>/docs/products/plans/`
- 功能属于特定项目（wopal、flex-scheduler、agent-tools）
- 代码修改范围仅在单一项目内
- 受益范围为单一项目

**2. 工作空间计划** → `docs/products/plans/`
- 涉及跨项目协作
- agent-tools 工具链升级（命令、规则、技能）
- 基础设施变更
- 所有项目受益的通用功能

### 文件命名规范

- `{feature-name-plan}.md`

### 示例

**项目计划**：
- `projects/web/wopal/docs/products/plans/add-oauth-login.md`
- `projects/python/flex-scheduler/docs/products/plans/cron-job.md`
- `projects/agent-tools/docs/products/plans/optimize-commit-command.md`

**工作空间计划**：
- `docs/products/plans/agent-tools-optimize-commit-command.md`（工具链升级）
- `docs/products/plans/workspace-add-worktree-manager.md`（基础设施）

---

## 质量检测标准

### 1. 上下文完整性
- 识别并记录了所有必要的代码模式
- 外部库使用附带文档链接和锚点
- 每个任务都配有可独立执行的验证命令

### 2. 准备就绪
- 任何另一位 Agent 甚至人类开发者无需额外上下文即可执行
- 任务严格按依赖顺序排列（从上到下执行无阻塞）
- 模式引用包含了具体的文件路径和行号
- **进度可追踪**: 每个子任务均自带 Checkbox 及状态标识

### 3. 模式一致性
- 新任务遵循代码库现有的最佳实践
- 不重复造轮子，复用现有的工具函数和类型

---

## 输出与报告

创建 Plan 文件后，请向用户简明扼要地报告：
1. **功能概述**：简述解决思路。
2. **文件路径**：输出创建好的 Plan 文件绝对路径。
3. **复杂度与风险**：指出主要的实现难点或风险考量。
4. **信心指数**：评估执行阶段 Agent 能够一次性成功实现该功能的信心分数（例如 8/10）。
5. **后续步骤**：询问用户是否需要审阅该计划，或直接进入执行阶段（如 `/execute`）。