1. Implement parsing of `.mdc` file metadata.
2. Update rule discovery to handle both `.md` and `.mdc` extensions.
3. Implement conditional logic to apply rules based on metadata.
4. If no metadata is present, the rules should always apply.
5. Add unit tests for the new functionality.
6. Update documentation to reflect the changes.
